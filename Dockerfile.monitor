FROM python:3.12-slim

WORKDIR /app

COPY worker_monitor.py .
RUN chmod +x worker_monitor.py

EXPOSE 8765

HEALTHCHECK --interval=30s --timeout=3s \
    CMD curl -f http://localhost:8765/ || exit 1

CMD ["python3", "worker_monitor.py"]
